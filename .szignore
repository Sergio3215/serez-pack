# Archivos/carpetas que NO se publican con `sz publish`.
# (sintaxis tipo .gitignore: nombres exactos, carpetas con / y globs con *)
# .git y este .szignore se excluyen siempre, automáticamente.

dist/
*.tmp
apps/
publish.ps1
